# running the file

```
mkdir build
cd build
cmake ..
make
```

This will prepare the files to be run

in order to run the files

To run the server (referee):
```
./comm_server
```

To run the (a) player
```
./comm_player
```